yt-dlp>=2024.0.0
youtube-transcript-api>=0.6.0
# Optional — only needed when running phases 2-4 via the SDK orchestrator
# (scripts/run_phase2.py, etc.). Copy-paste workflow does not require it.
# anthropic>=0.40.0
# Optional — only needed when YouTube captions are absent and Whisper must transcribe.
# openai-whisper>=20231117
# Optional — only needed for screen-heavy mode (frame dedup).
# imagehash>=4.3.1
# pillow>=10.0.0
