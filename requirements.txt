yt-dlp>=2024.0.0
youtube-transcript-api>=0.6.0
# Optional — only needed when YouTube captions are absent and Whisper must transcribe.
# openai-whisper>=20231117
# Optional — only needed for screen-heavy mode (frame dedup).
# imagehash>=4.3.1
# pillow>=10.0.0
