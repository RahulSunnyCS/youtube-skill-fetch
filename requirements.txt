yt-dlp>=2024.0.0
youtube-transcript-api>=0.6.0
# Optional — only needed when YouTube captions are missing and Whisper must
# transcribe the audio. faster-whisper is preferred (4x faster, half the memory,
# same model weights). openai-whisper works as a fallback.
# pip install faster-whisper        # recommended
# pip install openai-whisper        # fallback (older, slower)
# Optional — only needed when running phases 2-4 in mode="api" (direct
# Anthropic API calls). The default mode="claude_code" runs the work in
# your Claude Code session and does NOT require this package.
# anthropic>=0.40.0
# Optional — only needed when YouTube captions are absent and Whisper must transcribe.
# openai-whisper>=20231117
# Optional — only needed for screen-heavy mode (frame dedup).
# imagehash>=4.3.1
# pillow>=10.0.0
